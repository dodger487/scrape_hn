When using Scrimba, it would be nice to render any potential errors in the mini browser instead of the embedded console. But to do so can be cumbersome. This video demonstrates techniques you can use to tame potentially unsafe code in your applications.|||

When using Scrimba, it would be nice to render any potential errors in the mini browser instead of the embedded console.  But to do so can be cumbersome.  This video demonstrates techniques you can use to tame potentially unsafe code in your applications.