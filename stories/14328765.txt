Active Directory Agents will need to be recreated in Okta. Okta supports read / write to Active Directory

LDAP Agents will need to be recreated in Okta. Okta supports read / write to LDAP

Active Directory Agents will need to be recreated in Okta. Okta supports read / write to Active Directory

LDAP Agents will need to be recreated in Okta. Okta supports read / write to LDAP

During the migration, for each API Key (up to 10): The API Key ID & Secret are put into the next open slot on the Okta user profile (attribute is called “stormpathApiKey_[1-10]). Its format should be {apiKey.id}:{apiKey.secret}.|||

