Install the Python headers (TODO: specify the commands on every major OS.)

Install (prefix with if you need to):

Make sure you have Tox installed globally.|||

ohmu - View space usage in your terminal.