A three.js plugin for Mapbox GL JS, with support for basic animation and advanced 3D rendering.

Add it to your project via :

or download the bundle from and add include it in a tag on your page.

Set up and handle the core translations between a Three.js scene graph and the Mapbox GL JS map.

The object lets you load in a GeoJSON of points and will automatically place 3D models at each point.

These features currently have a dedicated interface in .

These features should be possible to implement using Three.js today but would benefit from a more geographic/map-specific interface in a future version of .

or to continually rebuild as you develop:

Both commands will output a bundled in .|||

threebox - A three.js plugin for Mapbox GL JS, with support for basic animation and advanced 3D rendering.