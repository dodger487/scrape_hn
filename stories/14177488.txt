The goal is to use RIPE Atlas Sensors as a source of DNS information for passive collection but also to provide additional information and statistics to Passive DNS users from the DNS measurements performed by the RIPE Atlas sensors.

The default stream is used and only the current measurement is gathered.

Don't forget to set your API key for Atlas via .

In parallel, you can generate statistics from the measurement gathered.

Specific values can be skip to avoid these in the statistics like .

generates a series of CSV files that can be ingested in D3.js bubble chart view.

Some more sample statistics available at https://www.foo.be/ripe-atlas generated during the hackathon.|||

passive-dns-atlas - Passive DNS collection (and statistics) from RIPE Atlas Sensors