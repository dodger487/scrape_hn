Converts UNIX-Timestamps to time from now in some social platforms style.

Library exports constructor. It accepts 2 string type params: first one tells what social platform style to use, and second one to define date format. Available styles are , and . Available date formats are (day, month, year) and (month, day, year).

returns an object with method. It accepts 1 param: Unix-timestamp as a number or a string. Another data types will throw an error.|||

Converts UNIX-Timestamps to time from now in some social platforms style.