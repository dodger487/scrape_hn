git log can be modified to get detailed information. Here are certain flag which can be associated:

This gives you the list of commits done by author for each line of file. Very useful to find the culprit in case error occurs.

git reset --soft HEAD^^^            #number of ^ decides number of commits you want to reset

git config -- alias.co checkout # use git co instead of git checkout git config -- alias.ci commit # use git ci instead of git commit git config -- alias.st status # use git st instead of git status git config -- alias.br branch # use git br instead of git branch git add -i staged unstaged path *** Commands *** 1: status 2: update 3: revert 4: add untracked 5: patch 6: diff 7: quit 8: help What now > That will be all from my side and are enough to manage projects. Good luck !|||

git, git tips, git branch, git log, git compare, compare between commits, git remote, git blame, git reset, git reset soft, git search commit message