A set of utility functions for taxonomies in WordPress.

amarkal-taxonomy lets you add custom fields to taxonomies in WordPress, and optionally add columns to the taxonomy terms table. More utility functions will be added in the future.

If you are using the command line:

Or simply add the following to your file:

This will install the package in the directory . Now all you need to do is include the composer autoloader.

Download amarkal-ui and amarkal-taxonomy from github and include them in your project.

Add a field to the add & edit forms of a given taxonomy.

This function can be used to add fields to the 'add term' and 'edit term' taxonomy forms. See amarkal-ui for supported field types, or register your own field type using .|||

amarkal-taxonomy - A set of utility functions for taxonomies in WordPress