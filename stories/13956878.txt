finds dead links on the web. It's useful for double-checking web pages for incorrect, or dead web links.

Inspired by Wikimedia and the Internet Archive fixing broken links on Wikipedia.

By default will only print links to pages that return something other than an .

If we specify the flag it will print all links:

The flag can be used to recurse into discovered links:

The flag can be used to avoid querying links from other domains:

If you're looking for specific HTTP return codes you can use the flag:|||

riplink - Find dead links on the web.