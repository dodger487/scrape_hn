By default calls to will be replaced with , but you can override this with a custom pragma (including itself when used with Preact).|||

babel-plugin-react-hyperscript - HyperScript syntax for React components without runtime overhead