This is a docker container which hosts an LDAP server. You can configure the settings for the LDAP server using the ldap-install.conf file. It uses Ubuntu 14.04 as the base image. Assumption is that Docker is setup.|||

Docker-LDAP - Dockerized LDAP server