Go ahead and enter a and for your repository. Since we want others to make use of this module as well, We will set it to .

We will also initialize a file, add file and

After entering all the details click on Create repository button. Now you will have a screen that looks like this.|||

In this tutorial, We will first write a Javascript module which will act as a database/store for storing data which we can use in our other frontend applications. And then we will publish it to npm [node package manager]