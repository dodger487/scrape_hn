An approach to define reusable and composable UI styles.

Having some base styles defined you can use them like so:

For a complete usage example see the Example project.

For the implementation details see and .

Defining the UI style in code is good because it gives

Because we can do better. By avoiding the use of the appearance-proxy we can access all properties and methods (not only and ), and nested objects!|||

StyleSheet - Reusable and composable UI styles