An autocomplete search box built with and for React.

The easiest way to use React-Search-Box is to install it from NPM and include it in your own React build process (using Browserify, etc).

At this point you can import react-search-box and its styles in your application as follows:

React-Search-Box can also listen for changes with the event property.

The 'data' property should be provided as an . The property of each option should be set to either a string or a number. The property of each option should be set to either a string or a number. It is the key that the search box will use for while iterating over your array of objects.

Have a look at the file for more details.

You can provide a custom prop to the component, for custom styling.

All sorts of contributions are welcome.|||

react-search-box - An autocomplete search box for ReactJS.