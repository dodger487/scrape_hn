These are Docker containers that include ansible latest stable 2.x version.

They are meant for testing purposes and are mainly used for dev-sec and the ansible-test-framework.|||

docker-ansible - Docker containers that include ansible latest stable 2.x version.