Press the button. The color has changed twice? This is implemented in the following code.

First, to prepare the style in primitive objects. Style values can be passed a string, number, function. In the case of function like a , it's evaluated just before, the return value is passed.

To build a motion by changing the style with timing control API. listen event emission, wait milliseconds for delay execution. It's easy to change the style. sets modular CSS, clear the set.

Repeat this two-step, to build an interaction. Specification of each API, please refer to the documentation.|||

