A collection of helpers for creating and modifying AWS account details that can not be done using any existing AWS API

Special Note: the operation requires that your IP be white-listed by AWS in order to bypass the CAPTCHA

Add this line to your application's Gemfile:

Or install it yourself as:

If you are required to go through a proxy, the following should be set prior to execution.

After checking out the repo, run to install dependencies. Then, run for an interactive prompt that will allow you to experiment.

To install this gem onto your local machine, run . To release a new version, update the version number in , and then run to create a git tag for the version, push git commits and tags, and push the file to rubygems.org.|||

aws_account_utils - Utility to help create and modify your AWS account