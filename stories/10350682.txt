// Possible values '', 'ssl', 'tls' - note TLS is not STARTTLS

// True turns on SMTP authentication, false turns it off

// SMTP authentication username, only used if WPMS_SMTP_AUTH is true

// SMTP authentication password, only used if WPMS_SMTP_AUTH is true

To enable SMTP emailing (use SMTP instead of mail()) in a WordPress Multisite, you can do this:(with the wp-config settings, the admin panel of wp-mail-smtp will be disabled)Cool!|||

