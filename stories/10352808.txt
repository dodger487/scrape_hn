A pure JavaScript library to get you up and running in no time with scroll events.

The scrollEvents API has managed not to change since the release, although we have expanded it with lots of goodies.

Let's looks at a simple example:

In this case we are changing the class after scrolling 10 pixels or more (measured by , is the default ).

That is just the tip of the iceberg though! Check out the API docs to get all the fancy notes.

is available via RawGit CDN (version ).

If you want to play with you can fork this fiddle:

Pull requests and issues are always welcome.|||

scrollEvents - :scroll: A pure JavaScript library to do stuff on scroll events. LibreJS compliant.