A React component for choosing dates and date ranges. Uses Moment.js for date operations.

, { } ; { } ; { ( ){ . (range); An object with two keys, 'startDate' and 'endDate' which are Momentjs objects. } (){ ( div DateRange onInit { . } onChange { . } div ) } }|||

react-date-range - A React component for choosing dates and date ranges.