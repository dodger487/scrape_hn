Add the following lines to your vimrc file:

For Python, we want multiline strings highlighted as comments rather than strings, because the background tint looks ugly on docstrings:|||

butter - A low-contrast color scheme for reading code all day