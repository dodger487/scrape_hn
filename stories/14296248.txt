jsx-transform-2-loader is a Webpack loader that transforms JSX decoupled from React (or any specific framework) using v2 of the package. It is based on which however uses an old version of .

Include the package as a loader in your Webpack config. You may specify all options that accepts. The most important option is the option (default: ), which determines the name of the factory function used to construct the Virtual DOM objects.|||

Configurable webpack loader for jsx-transform v2