is a simple bash script used for managing multiple Elasticsearch versions on your local machine.

Just download the script then make it executable

To uninstall, just remove the $EVM_HOME folder (usually ~/.evm)|||

evm - A Simple Elasticsearch Version Manager