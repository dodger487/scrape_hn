We are happy to announce initial support for the Cloudron CLI tool on Windows. The Cloudron CLI tool allows you to install, configure and test apps on your Cloudron.

If you already have Node.js installed on Windows, you can install the CLI tool using . Otherwise, you can follow this step by step guide to install the CLI tool on Windows.

You can download node.js for Windows from here. Be sure to download a Node.js version greater that 4.2.

By default, the installer installs Node.js under . You can either add the subdirectory to your default PATH or use the Node.js command prompt shortcut.

The cloudron CLI tool can be installed using .

You should now be able to login:

Try listing apps on your cloudron as follows:

To view the complete list of commands run .|||

This article announces Cloudron CLI support on Windows