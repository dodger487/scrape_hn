is a tool for con"cat"enating files. Reading a single file as input to a program is considered a Useless Use Of Cat (UUOC).

It's more efficient and less roundabout to simply use redirection. This is especially true for programs that can benefit from seekable input, like or .

Many tools also accept optional filenames, e.g. instead of .|||

shellcheck - ShellCheck, a static analysis tool for shell scripts